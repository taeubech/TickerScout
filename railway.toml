[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[[build.buildArgs]]
VITE_API_BASE_URL = "${{RAILWAY_PRIVATE_DOMAIN}}/api"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10